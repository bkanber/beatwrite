import React from "react";


export const Footer  = (props) => {
    return (
        <div className = "footer">
            <span><a href = "http://visgotti.com">© Visgotti 2017</a></span>
        </div>

    );
}

//<button onClick = { () => props.getSong(props.songId) }> { props.title } </button>
